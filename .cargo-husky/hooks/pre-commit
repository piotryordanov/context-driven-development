#!/bin/sh

# Run on pre-commit: fmt and clippy only

set -e

echo "🔍 Running pre-commit checks..."

echo "📝 Formatting code..."
cargo fmt --all -- --check

echo "🔍 Running clippy..."
cargo clippy --all-targets --all-features -- -D warnings

echo "✅ Pre-commit checks passed!"
